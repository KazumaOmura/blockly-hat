//作成、変更ファイル
//hat出力

// Do not edit this file; automatically generated by build.py.
'use strict';


Blockly.Hat = new Blockly.Generator("JavaScript");
Blockly.Hat.addReservedWords("Blockly,break,case,catch,continue,debugger,default,delete,do,else,finally,for,function,if,in,instanceof,new,return,switch,this,throw,try,typeof,var,void,while,with,class,enum,export,extends,import,super,implements,interface,let,package,private,protected,public,static,yield,const,null,true,false,Array,ArrayBuffer,Boolean,Date,decodeURI,decodeURIComponent,encodeURI,encodeURIComponent,Error,eval,EvalError,Float32Array,Float64Array,Function,Infinity,Int16Array,Int32Array,Int8Array,isFinite,isNaN,Iterator,JSON,Math,NaN,Number,Object,parseFloat,parseInt,RangeError,ReferenceError,RegExp,StopIteration,String,SyntaxError,TypeError,Uint16Array,Uint32Array,Uint8Array,Uint8ClampedArray,undefined,uneval,URIError,applicationCache,closed,Components,content,_content,controllers,crypto,defaultStatus,dialogArguments,directories,document,frameElement,frames,fullScreen,globalStorage,history,innerHeight,innerWidth,length,location,locationbar,localStorage,menubar,messageManager,mozAnimationStartTime,mozInnerScreenX,mozInnerScreenY,mozPaintCount,name,navigator,opener,outerHeight,outerWidth,pageXOffset,pageYOffset,parent,performance,personalbar,pkcs11,returnValue,screen,screenX,screenY,scrollbars,scrollMaxX,scrollMaxY,scrollX,scrollY,self,sessionStorage,sidebar,status,statusbar,toolbar,top,URL,window,addEventListener,alert,atob,back,blur,btoa,captureEvents,clearImmediate,clearInterval,clearTimeout,close,confirm,disableExternalCapture,dispatchEvent,dump,enableExternalCapture,escape,find,focus,forward,GeckoActiveXObject,getAttention,getAttentionWithCycleCount,getComputedStyle,getSelection,home,matchMedia,maximize,minimize,moveBy,moveTo,mozRequestAnimationFrame,open,openDialog,postMessage,print,prompt,QueryInterface,releaseEvents,removeEventListener,resizeBy,resizeTo,restore,routeEvent,scroll,scrollBy,scrollByLines,scrollByPages,scrollTo,setCursor,setImmediate,setInterval,setResizable,setTimeout,showModalDialog,sizeToContent,stop,unescape,updateCommands,XPCNativeWrapper,XPCSafeJSObjectWrapper,onabort,onbeforeunload,onblur,onchange,onclick,onclose,oncontextmenu,ondevicemotion,ondeviceorientation,ondragdrop,onerror,onfocus,onhashchange,onkeydown,onkeypress,onkeyup,onload,onmousedown,onmousemove,onmouseout,onmouseover,onmouseup,onmozbeforepaint,onpaint,onpopstate,onreset,onresize,onscroll,onselect,onsubmit,onunload,onpageshow,onpagehide,Image,Option,Worker,Event,Range,File,FileReader,Blob,BlobBuilder,Attr,CDATASection,CharacterData,Comment,console,DocumentFragment,DocumentType,DomConfiguration,DOMError,DOMErrorHandler,DOMException,DOMImplementation,DOMImplementationList,DOMImplementationRegistry,DOMImplementationSource,DOMLocator,DOMObject,DOMString,DOMStringList,DOMTimeStamp,DOMUserData,Entity,EntityReference,MediaQueryList,MediaQueryListListener,NameList,NamedNodeMap,Node,NodeFilter,NodeIterator,NodeList,Notation,Plugin,PluginArray,ProcessingInstruction,SharedWorker,Text,TimeRanges,Treewalker,TypeInfo,UserDataHandler,Worker,WorkerGlobalScope,HTMLDocument,HTMLElement,HTMLAnchorElement,HTMLAppletElement,HTMLAudioElement,HTMLAreaElement,HTMLBaseElement,HTMLBaseFontElement,HTMLBodyElement,HTMLBRElement,HTMLButtonElement,HTMLCanvasElement,HTMLDirectoryElement,HTMLDivElement,HTMLDListElement,HTMLEmbedElement,HTMLFieldSetElement,HTMLFontElement,HTMLFormElement,HTMLFrameElement,HTMLFrameSetElement,HTMLHeadElement,HTMLHeadingElement,HTMLHtmlElement,HTMLHRElement,HTMLIFrameElement,HTMLImageElement,HTMLInputElement,HTMLKeygenElement,HTMLLabelElement,HTMLLIElement,HTMLLinkElement,HTMLMapElement,HTMLMenuElement,HTMLMetaElement,HTMLModElement,HTMLObjectElement,HTMLOListElement,HTMLOptGroupElement,HTMLOptionElement,HTMLOutputElement,HTMLParagraphElement,HTMLParamElement,HTMLPreElement,HTMLQuoteElement,HTMLScriptElement,HTMLSelectElement,HTMLSourceElement,HTMLSpanElement,HTMLStyleElement,HTMLTableElement,HTMLTableCaptionElement,HTMLTableCellElement,HTMLTableDataCellElement,HTMLTableHeaderCellElement,HTMLTableColElement,HTMLTableRowElement,HTMLTableSectionElement,HTMLTextAreaElement,HTMLTimeElement,HTMLTitleElement,HTMLTrackElement,HTMLUListElement,HTMLUnknownElement,HTMLVideoElement,HTMLCanvasElement,CanvasRenderingContext2D,CanvasGradient,CanvasPattern,TextMetrics,ImageData,CanvasPixelArray,HTMLAudioElement,HTMLVideoElement,NotifyAudioAvailableEvent,HTMLCollection,HTMLAllCollection,HTMLFormControlsCollection,HTMLOptionsCollection,HTMLPropertiesCollection,DOMTokenList,DOMSettableTokenList,DOMStringMap,RadioNodeList,SVGDocument,SVGElement,SVGAElement,SVGAltGlyphElement,SVGAltGlyphDefElement,SVGAltGlyphItemElement,SVGAnimationElement,SVGAnimateElement,SVGAnimateColorElement,SVGAnimateMotionElement,SVGAnimateTransformElement,SVGSetElement,SVGCircleElement,SVGClipPathElement,SVGColorProfileElement,SVGCursorElement,SVGDefsElement,SVGDescElement,SVGEllipseElement,SVGFilterElement,SVGFilterPrimitiveStandardAttributes,SVGFEBlendElement,SVGFEColorMatrixElement,SVGFEComponentTransferElement,SVGFECompositeElement,SVGFEConvolveMatrixElement,SVGFEDiffuseLightingElement,SVGFEDisplacementMapElement,SVGFEDistantLightElement,SVGFEFloodElement,SVGFEGaussianBlurElement,SVGFEImageElement,SVGFEMergeElement,SVGFEMergeNodeElement,SVGFEMorphologyElement,SVGFEOffsetElement,SVGFEPointLightElement,SVGFESpecularLightingElement,SVGFESpotLightElement,SVGFETileElement,SVGFETurbulenceElement,SVGComponentTransferFunctionElement,SVGFEFuncRElement,SVGFEFuncGElement,SVGFEFuncBElement,SVGFEFuncAElement,SVGFontElement,SVGFontFaceElement,SVGFontFaceFormatElement,SVGFontFaceNameElement,SVGFontFaceSrcElement,SVGFontFaceUriElement,SVGForeignObjectElement,SVGGElement,SVGGlyphElement,SVGGlyphRefElement,SVGGradientElement,SVGLinearGradientElement,SVGRadialGradientElement,SVGHKernElement,SVGImageElement,SVGLineElement,SVGMarkerElement,SVGMaskElement,SVGMetadataElement,SVGMissingGlyphElement,SVGMPathElement,SVGPathElement,SVGPatternElement,SVGPolylineElement,SVGPolygonElement,SVGRectElement,SVGScriptElement,SVGStopElement,SVGStyleElement,SVGSVGElement,SVGSwitchElement,SVGSymbolElement,SVGTextElement,SVGTextPathElement,SVGTitleElement,SVGTRefElement,SVGTSpanElement,SVGUseElement,SVGViewElement,SVGVKernElement,SVGAngle,SVGColor,SVGICCColor,SVGElementInstance,SVGElementInstanceList,SVGLength,SVGLengthList,SVGMatrix,SVGNumber,SVGNumberList,SVGPaint,SVGPoint,SVGPointList,SVGPreserveAspectRatio,SVGRect,SVGStringList,SVGTransform,SVGTransformList,SVGAnimatedAngle,SVGAnimatedBoolean,SVGAnimatedEnumeration,SVGAnimatedInteger,SVGAnimatedLength,SVGAnimatedLengthList,SVGAnimatedNumber,SVGAnimatedNumberList,SVGAnimatedPreserveAspectRatio,SVGAnimatedRect,SVGAnimatedString,SVGAnimatedTransformList,SVGPathSegList,SVGPathSeg,SVGPathSegArcAbs,SVGPathSegArcRel,SVGPathSegClosePath,SVGPathSegCurvetoCubicAbs,SVGPathSegCurvetoCubicRel,SVGPathSegCurvetoCubicSmoothAbs,SVGPathSegCurvetoCubicSmoothRel,SVGPathSegCurvetoQuadraticAbs,SVGPathSegCurvetoQuadraticRel,SVGPathSegCurvetoQuadraticSmoothAbs,SVGPathSegCurvetoQuadraticSmoothRel,SVGPathSegLinetoAbs,SVGPathSegLinetoHorizontalAbs,SVGPathSegLinetoHorizontalRel,SVGPathSegLinetoRel,SVGPathSegLinetoVerticalAbs,SVGPathSegLinetoVerticalRel,SVGPathSegMovetoAbs,SVGPathSegMovetoRel,ElementTimeControl,TimeEvent,SVGAnimatedPathData,SVGAnimatedPoints,SVGColorProfileRule,SVGCSSRule,SVGExternalResourcesRequired,SVGFitToViewBox,SVGLangSpace,SVGLocatable,SVGRenderingIntent,SVGStylable,SVGTests,SVGTextContentElement,SVGTextPositioningElement,SVGTransformable,SVGUnitTypes,SVGURIReference,SVGViewSpec,SVGZoomAndPan");
Blockly.Hat.ORDER_ATOMIC = 0;
Blockly.Hat.ORDER_NEW = 1.1;
Blockly.Hat.ORDER_MEMBER = 1.2;
Blockly.Hat.ORDER_FUNCTION_CALL = 2;
Blockly.Hat.ORDER_INCREMENT = 3;
Blockly.Hat.ORDER_DECREMENT = 3;
Blockly.Hat.ORDER_BITWISE_NOT = 4.1;
Blockly.Hat.ORDER_UNARY_PLUS = 4.2;
Blockly.Hat.ORDER_UNARY_NEGATION = 4.3;
Blockly.Hat.ORDER_LOGICAL_NOT = 4.4;
Blockly.Hat.ORDER_TYPEOF = 4.5;
Blockly.Hat.ORDER_VOID = 4.6;
Blockly.Hat.ORDER_DELETE = 4.7;
Blockly.Hat.ORDER_DIVISION = 5.1;
Blockly.Hat.ORDER_MULTIPLICATION = 5.2;
Blockly.Hat.ORDER_MODULUS = 5.3;
Blockly.Hat.ORDER_SUBTRACTION = 6.1;
Blockly.Hat.ORDER_ADDITION = 6.2;
Blockly.Hat.ORDER_BITWISE_SHIFT = 7;
Blockly.Hat.ORDER_RELATIONAL = 8;
Blockly.Hat.ORDER_IN = 8;
Blockly.Hat.ORDER_INSTANCEOF = 8;
Blockly.Hat.ORDER_EQUALITY = 9;
Blockly.Hat.ORDER_BITWISE_AND = 10;
Blockly.Hat.ORDER_BITWISE_XOR = 11;
Blockly.Hat.ORDER_BITWISE_OR = 12;
Blockly.Hat.ORDER_LOGICAL_AND = 13;
Blockly.Hat.ORDER_LOGICAL_OR = 14;
Blockly.Hat.ORDER_CONDITIONAL = 15;
Blockly.Hat.ORDER_ASSIGNMENT = 16;
Blockly.Hat.ORDER_COMMA = 17;
Blockly.Hat.ORDER_NONE = 99;
Blockly.Hat.ORDER_OVERRIDES = [
    [Blockly.Hat.ORDER_FUNCTION_CALL, Blockly.Hat.ORDER_MEMBER],
    [Blockly.Hat.ORDER_FUNCTION_CALL, Blockly.Hat.ORDER_FUNCTION_CALL],
    [Blockly.Hat.ORDER_MEMBER, Blockly.Hat.ORDER_MEMBER],
    [Blockly.Hat.ORDER_MEMBER, Blockly.Hat.ORDER_FUNCTION_CALL],
    [Blockly.Hat.ORDER_LOGICAL_NOT, Blockly.Hat.ORDER_LOGICAL_NOT],
    [Blockly.Hat.ORDER_MULTIPLICATION, Blockly.Hat.ORDER_MULTIPLICATION],
    [Blockly.Hat.ORDER_ADDITION,
        Blockly.Hat.ORDER_ADDITION
    ],
    [Blockly.Hat.ORDER_LOGICAL_AND, Blockly.Hat.ORDER_LOGICAL_AND],
    [Blockly.Hat.ORDER_LOGICAL_OR, Blockly.Hat.ORDER_LOGICAL_OR]
];
Blockly.Hat.init = function (a) {
    Blockly.Hat.definitions_ = Object.create(null);
    Blockly.Hat.functionNames_ = Object.create(null);
    Blockly.Hat.variableDB_ ? Blockly.Hat.variableDB_.reset() : Blockly.Hat.variableDB_ = new Blockly.Names(Blockly.Hat.RESERVED_WORDS_);
    var b = [];
    a = a.getAllVariables();
    if (a.length) {
        for (var c = 0; c < a.length; c++) b[c] = Blockly.Hat.variableDB_.getName(a[c].name, Blockly.Variables.NAME_TYPE);
        Blockly.Hat.definitions_.variables = "var " + b.join(", ") +
            ";"
    }
};
Blockly.Hat.finish = function (a) {
    var b = [],
        c;
    for (c in Blockly.Hat.definitions_) b.push(Blockly.Hat.definitions_[c]);
    delete Blockly.Hat.definitions_;
    delete Blockly.Hat.functionNames_;
    Blockly.Hat.variableDB_.reset();
    return b.join("\n\n") + "\n\n\n" + a
};
Blockly.Hat.scrubNakedValue = function (a) {
    return a + ";\n"
};
Blockly.Hat.quote_ = function (a) {
    a = a.replace(/\\/g, "\\\\").replace(/\n/g, "\\\n").replace(/'/g, "\\'");
    return "'" + a + "'"
};
Blockly.Hat.scrub_ = function (a, b) {
    var c = "";
    if (!a.outputConnection || !a.outputConnection.targetConnection) {
        var d = a.getCommentText();
        (d = Blockly.utils.wrap(d, Blockly.Hat.COMMENT_WRAP - 3)) && (c = a.getProcedureDef ? c + ("/**\n" + Blockly.Hat.prefixLines(d + "\n", " * ") + " */\n") : c + Blockly.Hat.prefixLines(d + "\n", "// "));
        for (var e = 0; e < a.inputList.length; e++) a.inputList[e].type == Blockly.INPUT_VALUE && (d = a.inputList[e].connection.targetBlock()) && (d = Blockly.Hat.allNestedComments(d)) && (c +=
            Blockly.Hat.prefixLines(d, "// "))
    }
    e = a.nextConnection && a.nextConnection.targetBlock();
    e = Blockly.Hat.blockToCode(e);
    return c + b + e
};
Blockly.Hat.getAdjusted = function (a, b, c, d, e) {
    c = c || 0;
    e = e || Blockly.Hat.ORDER_NONE;
    a.workspace.options.oneBasedIndex && c--;
    var f = a.workspace.options.oneBasedIndex ? "1" : "0";
    a = 0 < c ? Blockly.Hat.valueToCode(a, b, Blockly.Hat.ORDER_ADDITION) || f : 0 > c ? Blockly.Hat.valueToCode(a, b, Blockly.Hat.ORDER_SUBTRACTION) || f : d ? Blockly.Hat.valueToCode(a, b, Blockly.Hat.ORDER_UNARY_NEGATION) || f : Blockly.Hat.valueToCode(a, b, e) || f;
    if (Blockly.isNumber(a)) a = parseFloat(a) + c,
        d && (a = -a);
    else {
        if (0 < c) {
            a = a + " + " + c;
            var g = Blockly.Hat.ORDER_ADDITION
        } else 0 > c && (a = a + " - " + -c, g = Blockly.Hat.ORDER_SUBTRACTION);
        d && (a = c ? "-(" + a + ")" : "-" + a, g = Blockly.Hat.ORDER_UNARY_NEGATION);
        g = Math.floor(g);
        e = Math.floor(e);
        g && e >= g && (a = "(" + a + ")")
    }
    return a
};
Blockly.Hat.colour = {};
Blockly.Hat.colour_picker = function (a) {
    return ["'" + a.getFieldValue("COLOUR") + "'", Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.colour_random = function (a) {
    return [Blockly.Hat.provideFunction_("colourRandom", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "() {", "  var num = Math.floor(Math.random() * Math.pow(2, 24));", "  return '#' + ('00000' + num.toString(16)).substr(-6);", "}"]) + "()", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.colour_rgb = function (a) {
    var b = Blockly.Hat.valueToCode(a, "RED", Blockly.Hat.ORDER_COMMA) || 0,
        c = Blockly.Hat.valueToCode(a, "GREEN", Blockly.Hat.ORDER_COMMA) || 0;
    a = Blockly.Hat.valueToCode(a, "BLUE", Blockly.Hat.ORDER_COMMA) || 0;
    return [Blockly.Hat.provideFunction_("colourRgb", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(r, g, b) {", "  r = Math.max(Math.min(Number(r), 100), 0) * 2.55;", "  g = Math.max(Math.min(Number(g), 100), 0) * 2.55;",
        "  b = Math.max(Math.min(Number(b), 100), 0) * 2.55;", "  r = ('0' + (Math.round(r) || 0).toString(16)).slice(-2);", "  g = ('0' + (Math.round(g) || 0).toString(16)).slice(-2);", "  b = ('0' + (Math.round(b) || 0).toString(16)).slice(-2);", "  return '#' + r + g + b;", "}"
    ]) + "(" + b + ", " + c + ", " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.colour_blend = function (a) {
    var b = Blockly.Hat.valueToCode(a, "COLOUR1", Blockly.Hat.ORDER_COMMA) || "'#000000'",
        c = Blockly.Hat.valueToCode(a, "COLOUR2", Blockly.Hat.ORDER_COMMA) || "'#000000'";
    a = Blockly.Hat.valueToCode(a, "RATIO", Blockly.Hat.ORDER_COMMA) || .5;
    return [Blockly.Hat.provideFunction_("colourBlend", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(c1, c2, ratio) {", "  ratio = Math.max(Math.min(Number(ratio), 1), 0);", "  var r1 = parseInt(c1.substring(1, 3), 16);",
        "  var g1 = parseInt(c1.substring(3, 5), 16);", "  var b1 = parseInt(c1.substring(5, 7), 16);", "  var r2 = parseInt(c2.substring(1, 3), 16);", "  var g2 = parseInt(c2.substring(3, 5), 16);", "  var b2 = parseInt(c2.substring(5, 7), 16);", "  var r = Math.round(r1 * (1 - ratio) + r2 * ratio);", "  var g = Math.round(g1 * (1 - ratio) + g2 * ratio);", "  var b = Math.round(b1 * (1 - ratio) + b2 * ratio);", "  r = ('0' + (r || 0).toString(16)).slice(-2);", "  g = ('0' + (g || 0).toString(16)).slice(-2);", "  b = ('0' + (b || 0).toString(16)).slice(-2);",
        "  return '#' + r + g + b;", "}"
    ]) + "(" + b + ", " + c + ", " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists = {};
Blockly.Hat.lists_create_empty = function (a) {
    return ["[]", Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.lists_create_with = function (a) {
    for (var b = Array(a.itemCount_), c = 0; c < a.itemCount_; c++) b[c] = Blockly.Hat.valueToCode(a, "ADD" + c, Blockly.Hat.ORDER_COMMA) || "null";
    return ["[" + b.join(", ") + "]", Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.lists_repeat = function (a) {
    var b = Blockly.Hat.provideFunction_("listsRepeat", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(value, n) {", "  var array = [];", "  for (var i = 0; i < n; i++) {", "    array[i] = value;", "  }", "  return array;", "}"]),
        c = Blockly.Hat.valueToCode(a, "ITEM", Blockly.Hat.ORDER_COMMA) || "null";
    a = Blockly.Hat.valueToCode(a, "NUM", Blockly.Hat.ORDER_COMMA) || "0";
    return [b + "(" + c + ", " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists_length = function (a) {
    return [(Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_MEMBER) || "[]") + ".length", Blockly.Hat.ORDER_MEMBER]
};
Blockly.Hat.lists_isEmpty = function (a) {
    return ["!" + (Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_MEMBER) || "[]") + ".length", Blockly.Hat.ORDER_LOGICAL_NOT]
};
Blockly.Hat.lists_indexOf = function (a) {
    var b = "FIRST" == a.getFieldValue("END") ? "indexOf" : "lastIndexOf",
        c = Blockly.Hat.valueToCode(a, "FIND", Blockly.Hat.ORDER_NONE) || "''";
    b = (Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_MEMBER) || "[]") + "." + b + "(" + c + ")";
    return a.workspace.options.oneBasedIndex ? [b + " + 1", Blockly.Hat.ORDER_ADDITION] : [b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists_getIndex = function (a) {
    var b = a.getFieldValue("MODE") || "GET",
        c = a.getFieldValue("WHERE") || "FROM_START",
        d = Blockly.Hat.valueToCode(a, "VALUE", "RANDOM" == c ? Blockly.Hat.ORDER_COMMA : Blockly.Hat.ORDER_MEMBER) || "[]";
    switch (c) {
        case "FIRST":
            if ("GET" == b) return [d + "[0]", Blockly.Hat.ORDER_MEMBER];
            if ("GET_REMOVE" == b) return [d + ".shift()", Blockly.Hat.ORDER_MEMBER];
            if ("REMOVE" == b) return d + ".shift();\n";
            break;
        case "LAST":
            if ("GET" == b) return [d + ".slice(-1)[0]", Blockly.Hat.ORDER_MEMBER];
            if ("GET_REMOVE" == b) return [d + ".pop()", Blockly.Hat.ORDER_MEMBER];
            if ("REMOVE" == b) return d + ".pop();\n";
            break;
        case "FROM_START":
            a = Blockly.Hat.getAdjusted(a, "AT");
            if ("GET" == b) return [d + "[" + a + "]", Blockly.Hat.ORDER_MEMBER];
            if ("GET_REMOVE" == b) return [d + ".splice(" + a + ", 1)[0]", Blockly.Hat.ORDER_FUNCTION_CALL];
            if ("REMOVE" == b) return d + ".splice(" + a + ", 1);\n";
            break;
        case "FROM_END":
            a = Blockly.Hat.getAdjusted(a, "AT", 1, !0);
            if ("GET" == b) return [d + ".slice(" + a + ")[0]", Blockly.Hat.ORDER_FUNCTION_CALL];
            if ("GET_REMOVE" == b) return [d + ".splice(" + a + ", 1)[0]", Blockly.Hat.ORDER_FUNCTION_CALL];
            if ("REMOVE" == b) return d + ".splice(" + a + ", 1);";
            break;
        case "RANDOM":
            d = Blockly.Hat.provideFunction_("listsGetRandomItem", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(list, remove) {", "  var x = Math.floor(Math.random() * list.length);", "  if (remove) {", "    return list.splice(x, 1)[0];", "  } else {", "    return list[x];", "  }", "}"]) + "(" + d + ", " + ("GET" != b) + ")";
            if ("GET" == b || "GET_REMOVE" == b) return [d,
                Blockly.Hat.ORDER_FUNCTION_CALL
            ];
            if ("REMOVE" == b) return d + ";\n"
    }
    throw "Unhandled combination (lists_getIndex).";
};
Blockly.Hat.lists_setIndex = function (a) {
    function b() {
        if (c.match(/^\w+$/)) return "";
        var a = Blockly.Hat.variableDB_.getDistinctName("tmpList", Blockly.Variables.NAME_TYPE),
            b = "var " + a + " = " + c + ";\n";
        c = a;
        return b
    }
    var c = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_MEMBER) || "[]",
        d = a.getFieldValue("MODE") || "GET",
        e = a.getFieldValue("WHERE") || "FROM_START",
        f = Blockly.Hat.valueToCode(a, "TO", Blockly.Hat.ORDER_ASSIGNMENT) || "null";
    switch (e) {
        case "FIRST":
            if ("SET" == d) return c +
                "[0] = " + f + ";\n";
            if ("INSERT" == d) return c + ".unshift(" + f + ");\n";
            break;
        case "LAST":
            if ("SET" == d) return a = b(), a + (c + "[" + c + ".length - 1] = " + f + ";\n");
            if ("INSERT" == d) return c + ".push(" + f + ");\n";
            break;
        case "FROM_START":
            e = Blockly.Hat.getAdjusted(a, "AT");
            if ("SET" == d) return c + "[" + e + "] = " + f + ";\n";
            if ("INSERT" == d) return c + ".splice(" + e + ", 0, " + f + ");\n";
            break;
        case "FROM_END":
            e = Blockly.Hat.getAdjusted(a, "AT", 1, !1, Blockly.Hat.ORDER_SUBTRACTION);
            a = b();
            if ("SET" == d) return a + (c + "[" + c + ".length - " + e +
                "] = " + f + ";\n");
            if ("INSERT" == d) return a + (c + ".splice(" + c + ".length - " + e + ", 0, " + f + ");\n");
            break;
        case "RANDOM":
            a = b();
            e = Blockly.Hat.variableDB_.getDistinctName("tmpX", Blockly.Variables.NAME_TYPE);
            a += "var " + e + " = Math.floor(Math.random() * " + c + ".length);\n";
            if ("SET" == d) return a + (c + "[" + e + "] = " + f + ";\n");
            if ("INSERT" == d) return a + (c + ".splice(" + e + ", 0, " + f + ");\n")
    }
    throw "Unhandled combination (lists_setIndex).";
};
Blockly.Hat.lists.getIndex_ = function (a, b, c) {
    return "FIRST" == b ? "0" : "FROM_END" == b ? a + ".length - 1 - " + c : "LAST" == b ? a + ".length - 1" : c
};
Blockly.Hat.lists_getSublist = function (a) {
    var b = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_MEMBER) || "[]",
        c = a.getFieldValue("WHERE1"),
        d = a.getFieldValue("WHERE2");
    if ("FIRST" == c && "LAST" == d) b += ".slice(0)";
    else if (b.match(/^\w+$/) || "FROM_END" != c && "FROM_START" == d) {
        switch (c) {
            case "FROM_START":
                var e = Blockly.Hat.getAdjusted(a, "AT1");
                break;
            case "FROM_END":
                e = Blockly.Hat.getAdjusted(a, "AT1", 1, !1, Blockly.Hat.ORDER_SUBTRACTION);
                e = b + ".length - " + e;
                break;
            case "FIRST":
                e =
                    "0";
                break;
            default:
                throw "Unhandled option (lists_getSublist).";
        }
        switch (d) {
            case "FROM_START":
                a = Blockly.Hat.getAdjusted(a, "AT2", 1);
                break;
            case "FROM_END":
                a = Blockly.Hat.getAdjusted(a, "AT2", 0, !1, Blockly.Hat.ORDER_SUBTRACTION);
                a = b + ".length - " + a;
                break;
            case "LAST":
                a = b + ".length";
                break;
            default:
                throw "Unhandled option (lists_getSublist).";
        }
        b = b + ".slice(" + e + ", " + a + ")"
    } else {
        e = Blockly.Hat.getAdjusted(a, "AT1");
        a = Blockly.Hat.getAdjusted(a, "AT2");
        var f = Blockly.Hat.lists.getIndex_,
            g = {
                FIRST: "First",
                LAST: "Last",
                FROM_START: "FromStart",
                FROM_END: "FromEnd"
            };
        b = Blockly.Hat.provideFunction_("subsequence" + g[c] + g[d], ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(sequence" + ("FROM_END" == c || "FROM_START" == c ? ", at1" : "") + ("FROM_END" == d || "FROM_START" == d ? ", at2" : "") + ") {", "  var start = " + f("sequence", c, "at1") + ";", "  var end = " + f("sequence", d, "at2") + " + 1;", "  return sequence.slice(start, end);", "}"]) + "(" + b + ("FROM_END" == c || "FROM_START" == c ? ", " + e : "") + ("FROM_END" == d || "FROM_START" ==
            d ? ", " + a : "") + ")"
    }
    return [b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists_sort = function (a) {
    var b = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_FUNCTION_CALL) || "[]",
        c = "1" === a.getFieldValue("DIRECTION") ? 1 : -1;
    a = a.getFieldValue("TYPE");
    var d = Blockly.Hat.provideFunction_("listsGetSortCompare", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(type, direction) {", "  var compareFuncs = {", '    "NUMERIC": function(a, b) {', "        return parseFloat(a) - parseFloat(b); },", '    "TEXT": function(a, b) {', "        return a.toString() > b.toString() ? 1 : -1; },",
        '    "IGNORE_CASE": function(a, b) {', "        return a.toString().toLowerCase() > b.toString().toLowerCase() ? 1 : -1; },", "  };", "  var compare = compareFuncs[type];", "  return function(a, b) { return compare(a, b) * direction; }", "}"
    ]);
    return [b + ".slice().sort(" + d + '("' + a + '", ' + c + "))", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists_split = function (a) {
    var b = Blockly.Hat.valueToCode(a, "INPUT", Blockly.Hat.ORDER_MEMBER),
        c = Blockly.Hat.valueToCode(a, "DELIM", Blockly.Hat.ORDER_NONE) || "''";
    a = a.getFieldValue("MODE");
    if ("SPLIT" == a) b || (b = "''"), a = "split";
    else if ("JOIN" == a) b || (b = "[]"), a = "join";
    else throw "Unknown mode: " + a;
    return [b + "." + a + "(" + c + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.lists_reverse = function (a) {
    return [(Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_FUNCTION_CALL) || "[]") + ".slice().reverse()", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.logic = {};
Blockly.Hat.controls_if = function (a) {
    var b = 0,
        c = "";
    do {
        var d = Blockly.Hat.valueToCode(a, "IF" + b, Blockly.Hat.ORDER_NONE) || "false";
        var e = Blockly.Hat.statementToCode(a, "DO" + b);
        c += (0 < b ? " else " : "") + "if (" + d + ") {\n" + e + "}";
        ++b
    } while (a.getInput("IF" + b));
    a.getInput("ELSE") && (e = Blockly.Hat.statementToCode(a, "ELSE"), c += " else {\n" + e + "}");
    return c + "\n"
};
Blockly.Hat.controls_ifelse = Blockly.Hat.controls_if;
Blockly.Hat.logic_compare = function (a) {
    var b = {
            EQ: "==",
            NEQ: "!=",
            LT: "<",
            LTE: "<=",
            GT: ">",
            GTE: ">="
        } [a.getFieldValue("OP")],
        c = "==" == b || "!=" == b ? Blockly.Hat.ORDER_EQUALITY : Blockly.Hat.ORDER_RELATIONAL,
        d = Blockly.Hat.valueToCode(a, "A", c) || "0";
    a = Blockly.Hat.valueToCode(a, "B", c) || "0";
    return [d + " " + b + " " + a, c]
};
Blockly.Hat.logic_operation = function (a) {
    var b = "AND" == a.getFieldValue("OP") ? "&&" : "||",
        c = "&&" == b ? Blockly.Hat.ORDER_LOGICAL_AND : Blockly.Hat.ORDER_LOGICAL_OR,
        d = Blockly.Hat.valueToCode(a, "A", c);
    a = Blockly.Hat.valueToCode(a, "B", c);
    if (d || a) {
        var e = "&&" == b ? "true" : "false";
        d || (d = e);
        a || (a = e)
    } else a = d = "false";
    return [d + " " + b + " " + a, c]
};
Blockly.Hat.logic_negate = function (a) {
    var b = Blockly.Hat.ORDER_LOGICAL_NOT;
    return ["!" + (Blockly.Hat.valueToCode(a, "BOOL", b) || "true"), b]
};
Blockly.Hat.logic_boolean = function (a) {
    return ["TRUE" == a.getFieldValue("BOOL") ? "true" : "false", Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.logic_null = function (a) {
    return ["null", Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.logic_ternary = function (a) {
    var b = Blockly.Hat.valueToCode(a, "IF", Blockly.Hat.ORDER_CONDITIONAL) || "false",
        c = Blockly.Hat.valueToCode(a, "THEN", Blockly.Hat.ORDER_CONDITIONAL) || "null";
    a = Blockly.Hat.valueToCode(a, "ELSE", Blockly.Hat.ORDER_CONDITIONAL) || "null";
    return [b + " ? " + c + " : " + a, Blockly.Hat.ORDER_CONDITIONAL]
};
Blockly.Hat.loops = {};
Blockly.Hat.controls_repeat_ext = function (a) {
    var b = a.getField("TIMES") ? String(Number(a.getFieldValue("TIMES"))) : Blockly.Hat.valueToCode(a, "TIMES", Blockly.Hat.ORDER_ASSIGNMENT) || "0",
        c = Blockly.Hat.statementToCode(a, "DO");
    c = Blockly.Hat.addLoopTrap(c, a.id);
    a = "";
    var d = Blockly.Hat.variableDB_.getDistinctName("count", Blockly.Variables.NAME_TYPE),
        e = b;
    b.match(/^\w+$/) || Blockly.isNumber(b) || (e = Blockly.Hat.variableDB_.getDistinctName("repeat_end", Blockly.Variables.NAME_TYPE),
        a += "var " + e + " = " + b + ";\n");
    return a + ("for (var " + d + " = 0; " + d + " < " + e + "; " + d + "++) {\n" + c + "}\n")
};
Blockly.Hat.controls_repeat = Blockly.Hat.controls_repeat_ext;
Blockly.Hat.controls_whileUntil = function (a) {
    var b = "UNTIL" == a.getFieldValue("MODE"),
        c = Blockly.Hat.valueToCode(a, "BOOL", b ? Blockly.Hat.ORDER_LOGICAL_NOT : Blockly.Hat.ORDER_NONE) || "false",
        d = Blockly.Hat.statementToCode(a, "DO");
    d = Blockly.Hat.addLoopTrap(d, a.id);
    b && (c = "!" + c);
    return "while (" + c + ") {\n" + d + "}\n"
};
Blockly.Hat.controls_for = function (a) {
    var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE),
        c = Blockly.Hat.valueToCode(a, "FROM", Blockly.Hat.ORDER_ASSIGNMENT) || "0",
        d = Blockly.Hat.valueToCode(a, "TO", Blockly.Hat.ORDER_ASSIGNMENT) || "0",
        e = Blockly.Hat.valueToCode(a, "BY", Blockly.Hat.ORDER_ASSIGNMENT) || "1",
        f = Blockly.Hat.statementToCode(a, "DO");
    f = Blockly.Hat.addLoopTrap(f, a.id);
    if (Blockly.isNumber(c) && Blockly.isNumber(d) &&
        Blockly.isNumber(e)) {
        var g = parseFloat(c) <= parseFloat(d);
        a = "for (" + b + " = " + c + "; " + b + (g ? " <= " : " >= ") + d + "; " + b;
        b = Math.abs(parseFloat(e));
        a = (1 == b ? a + (g ? "++" : "--") : a + ((g ? " += " : " -= ") + b)) + (") {\n" + f + "}\n")
    } else a = "", g = c, c.match(/^\w+$/) || Blockly.isNumber(c) || (g = Blockly.Hat.variableDB_.getDistinctName(b + "_start", Blockly.Variables.NAME_TYPE), a += "var " + g + " = " + c + ";\n"), c = d, d.match(/^\w+$/) || Blockly.isNumber(d) || (c = Blockly.Hat.variableDB_.getDistinctName(b + "_end", Blockly.Variables.NAME_TYPE),
        a += "var " + c + " = " + d + ";\n"), d = Blockly.Hat.variableDB_.getDistinctName(b + "_inc", Blockly.Variables.NAME_TYPE), a += "var " + d + " = ", a = Blockly.isNumber(e) ? a + (Math.abs(e) + ";\n") : a + ("Math.abs(" + e + ");\n"), a = a + ("if (" + g + " > " + c + ") {\n") + (Blockly.Hat.INDENT + d + " = -" + d + ";\n"), a += "}\n", a += "for (" + b + " = " + g + "; " + d + " >= 0 ? " + b + " <= " + c + " : " + b + " >= " + c + "; " + b + " += " + d + ") {\n" + f + "}\n";
    return a
};
Blockly.Hat.controls_forEach = function (a) {
    var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE),
        c = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_ASSIGNMENT) || "[]",
        d = Blockly.Hat.statementToCode(a, "DO");
    d = Blockly.Hat.addLoopTrap(d, a.id);
    a = "";
    var e = c;
    c.match(/^\w+$/) || (e = Blockly.Hat.variableDB_.getDistinctName(b + "_list", Blockly.Variables.NAME_TYPE), a += "var " + e + " = " + c + ";\n");
    c = Blockly.Hat.variableDB_.getDistinctName(b +
        "_index", Blockly.Variables.NAME_TYPE);
    d = Blockly.Hat.INDENT + b + " = " + e + "[" + c + "];\n" + d;
    return a + ("for (var " + c + " in " + e + ") {\n" + d + "}\n")
};
Blockly.Hat.controls_flow_statements = function (a) {
    switch (a.getFieldValue("FLOW")) {
        case "BREAK":
            return "break;\n";
        case "CONTINUE":
            return "continue;\n"
    }
    throw "Unknown flow statement.";
};
Blockly.Hat.math = {};
Blockly.Hat.math_number = function (a) {
    return [parseFloat(a.getFieldValue("NUM")), Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.math_arithmetic = function (a) {
    var b = {
            ADD: [" + ", Blockly.Hat.ORDER_ADDITION],
            MINUS: [" - ", Blockly.Hat.ORDER_SUBTRACTION],
            MULTIPLY: [" * ", Blockly.Hat.ORDER_MULTIPLICATION],
            DIVIDE: [" / ", Blockly.Hat.ORDER_DIVISION],
            POWER: [null, Blockly.Hat.ORDER_COMMA]
        } [a.getFieldValue("OP")],
        c = b[0];
    b = b[1];
    var d = Blockly.Hat.valueToCode(a, "A", b) || "0";
    a = Blockly.Hat.valueToCode(a, "B", b) || "0";
    return c ? [d + c + a, b] : ["Math.pow(" + d + ", " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.math_single = function (a) {
    var b = a.getFieldValue("OP");
    if ("NEG" == b) return a = Blockly.Hat.valueToCode(a, "NUM", Blockly.Hat.ORDER_UNARY_NEGATION) || "0", "-" == a[0] && (a = " " + a), ["-" + a, Blockly.Hat.ORDER_UNARY_NEGATION];
    a = "SIN" == b || "COS" == b || "TAN" == b ? Blockly.Hat.valueToCode(a, "NUM", Blockly.Hat.ORDER_DIVISION) || "0" : Blockly.Hat.valueToCode(a, "NUM", Blockly.Hat.ORDER_NONE) || "0";
    switch (b) {
        case "ABS":
            var c = "Math.abs(" + a + ")";
            break;
        case "ROOT":
            c = "Math.sqrt(" +
                a + ")";
            break;
        case "LN":
            c = "Math.log(" + a + ")";
            break;
        case "EXP":
            c = "Math.exp(" + a + ")";
            break;
        case "POW10":
            c = "Math.pow(10," + a + ")";
            break;
        case "ROUND":
            c = "Math.round(" + a + ")";
            break;
        case "ROUNDUP":
            c = "Math.ceil(" + a + ")";
            break;
        case "ROUNDDOWN":
            c = "Math.floor(" + a + ")";
            break;
        case "SIN":
            c = "Math.sin(" + a + " / 180 * Math.PI)";
            break;
        case "COS":
            c = "Math.cos(" + a + " / 180 * Math.PI)";
            break;
        case "TAN":
            c = "Math.tan(" + a + " / 180 * Math.PI)"
    }
    if (c) return [c, Blockly.Hat.ORDER_FUNCTION_CALL];
    switch (b) {
        case "LOG10":
            c = "Math.log(" + a +
                ") / Math.log(10)";
            break;
        case "ASIN":
            c = "Math.asin(" + a + ") / Math.PI * 180";
            break;
        case "ACOS":
            c = "Math.acos(" + a + ") / Math.PI * 180";
            break;
        case "ATAN":
            c = "Math.atan(" + a + ") / Math.PI * 180";
            break;
        default:
            throw "Unknown math operator: " + b;
    }
    return [c, Blockly.Hat.ORDER_DIVISION]
};
Blockly.Hat.math_constant = function (a) {
    return {
        PI: ["Math.PI", Blockly.Hat.ORDER_MEMBER],
        E: ["Math.E", Blockly.Hat.ORDER_MEMBER],
        GOLDEN_RATIO: ["(1 + Math.sqrt(5)) / 2", Blockly.Hat.ORDER_DIVISION],
        SQRT2: ["Math.SQRT2", Blockly.Hat.ORDER_MEMBER],
        SQRT1_2: ["Math.SQRT1_2", Blockly.Hat.ORDER_MEMBER],
        INFINITY: ["Infinity", Blockly.Hat.ORDER_ATOMIC]
    } [a.getFieldValue("CONSTANT")]
};
Blockly.Hat.math_number_property = function (a) {
    var b = Blockly.Hat.valueToCode(a, "NUMBER_TO_CHECK", Blockly.Hat.ORDER_MODULUS) || "0",
        c = a.getFieldValue("PROPERTY");
    if ("PRIME" == c) return [Blockly.Hat.provideFunction_("mathIsPrime", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(n) {", "  // https://en.wikipedia.org/wiki/Primality_test#Naive_methods", "  if (n == 2 || n == 3) {", "    return true;", "  }", "  // False if n is NaN, negative, is 1, or not whole.", "  // And false if n is divisible by 2 or 3.",
        "  if (isNaN(n) || n <= 1 || n % 1 != 0 || n % 2 == 0 || n % 3 == 0) {", "    return false;", "  }", "  // Check all the numbers of form 6k +/- 1, up to sqrt(n).", "  for (var x = 6; x <= Math.sqrt(n) + 1; x += 6) {", "    if (n % (x - 1) == 0 || n % (x + 1) == 0) {", "      return false;", "    }", "  }", "  return true;", "}"
    ]) + "(" + b + ")", Blockly.Hat.ORDER_FUNCTION_CALL];
    switch (c) {
        case "EVEN":
            var d = b + " % 2 == 0";
            break;
        case "ODD":
            d = b + " % 2 == 1";
            break;
        case "WHOLE":
            d = b + " % 1 == 0";
            break;
        case "POSITIVE":
            d =
                b + " > 0";
            break;
        case "NEGATIVE":
            d = b + " < 0";
            break;
        case "DIVISIBLE_BY":
            a = Blockly.Hat.valueToCode(a, "DIVISOR", Blockly.Hat.ORDER_MODULUS) || "0", d = b + " % " + a + " == 0"
    }
    return [d, Blockly.Hat.ORDER_EQUALITY]
};
Blockly.Hat.math_change = function (a) {
    var b = Blockly.Hat.valueToCode(a, "DELTA", Blockly.Hat.ORDER_ADDITION) || "0";
    a = Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE);
    return a + " = (typeof " + a + " == 'number' ? " + a + " : 0) + " + b + ";\n"
};
Blockly.Hat.math_round = Blockly.Hat.math_single;
Blockly.Hat.math_trig = Blockly.Hat.math_single;
Blockly.Hat.math_on_list = function (a) {
    var b = a.getFieldValue("OP");
    switch (b) {
        case "SUM":
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_MEMBER) || "[]";
            a += ".reduce(function(x, y) {return x + y;})";
            break;
        case "MIN":
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_COMMA) || "[]";
            a = "Math.min.apply(null, " + a + ")";
            break;
        case "MAX":
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_COMMA) || "[]";
            a = "Math.max.apply(null, " + a + ")";
            break;
        case "AVERAGE":
            b = Blockly.Hat.provideFunction_("mathMean",
                ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(myList) {", "  return myList.reduce(function(x, y) {return x + y;}) / myList.length;", "}"]);
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_NONE) || "[]";
            a = b + "(" + a + ")";
            break;
        case "MEDIAN":
            b = Blockly.Hat.provideFunction_("mathMedian", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(myList) {", "  var localList = myList.filter(function (x) {return typeof x == 'number';});", "  if (!localList.length) return null;",
                "  localList.sort(function(a, b) {return b - a;});", "  if (localList.length % 2 == 0) {", "    return (localList[localList.length / 2 - 1] + localList[localList.length / 2]) / 2;", "  } else {", "    return localList[(localList.length - 1) / 2];", "  }", "}"
            ]);
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_NONE) || "[]";
            a = b + "(" + a + ")";
            break;
        case "MODE":
            b = Blockly.Hat.provideFunction_("mathModes", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(values) {", "  var modes = [];",
                "  var counts = [];", "  var maxCount = 0;", "  for (var i = 0; i < values.length; i++) {", "    var value = values[i];", "    var found = false;", "    var thisCount;", "    for (var j = 0; j < counts.length; j++) {", "      if (counts[j][0] === value) {", "        thisCount = ++counts[j][1];", "        found = true;", "        break;", "      }", "    }", "    if (!found) {", "      counts.push([value, 1]);", "      thisCount = 1;", "    }", "    maxCount = Math.max(thisCount, maxCount);", "  }", "  for (var j = 0; j < counts.length; j++) {",
                "    if (counts[j][1] == maxCount) {", "        modes.push(counts[j][0]);", "    }", "  }", "  return modes;", "}"
            ]);
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_NONE) || "[]";
            a = b + "(" + a + ")";
            break;
        case "STD_DEV":
            b = Blockly.Hat.provideFunction_("mathStandardDeviation", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(numbers) {", "  var n = numbers.length;", "  if (!n) return null;", "  var mean = numbers.reduce(function(x, y) {return x + y;}) / n;", "  var variance = 0;",
                "  for (var j = 0; j < n; j++) {", "    variance += Math.pow(numbers[j] - mean, 2);", "  }", "  variance = variance / n;", "  return Math.sqrt(variance);", "}"
            ]);
            a = Blockly.Hat.valueToCode(a, "LIST", Blockly.Hat.ORDER_NONE) || "[]";
            a = b + "(" + a + ")";
            break;
        case "RANDOM":
            b = Blockly.Hat.provideFunction_("mathRandomList", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(list) {", "  var x = Math.floor(Math.random() * list.length);", "  return list[x];", "}"]);
            a = Blockly.Hat.valueToCode(a,
                "LIST", Blockly.Hat.ORDER_NONE) || "[]";
            a = b + "(" + a + ")";
            break;
        default:
            throw "Unknown operator: " + b;
    }
    return [a, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.math_modulo = function (a) {
    var b = Blockly.Hat.valueToCode(a, "DIVIDEND", Blockly.Hat.ORDER_MODULUS) || "0";
    a = Blockly.Hat.valueToCode(a, "DIVISOR", Blockly.Hat.ORDER_MODULUS) || "0";
    return [b + " % " + a, Blockly.Hat.ORDER_MODULUS]
};
Blockly.Hat.math_constrain = function (a) {
    var b = Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_COMMA) || "0",
        c = Blockly.Hat.valueToCode(a, "LOW", Blockly.Hat.ORDER_COMMA) || "0";
    a = Blockly.Hat.valueToCode(a, "HIGH", Blockly.Hat.ORDER_COMMA) || "Infinity";
    return ["Math.min(Math.max(" + b + ", " + c + "), " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.math_random_int = function (a) {
    var b = Blockly.Hat.valueToCode(a, "FROM", Blockly.Hat.ORDER_COMMA) || "0";
    a = Blockly.Hat.valueToCode(a, "TO", Blockly.Hat.ORDER_COMMA) || "0";
    return [Blockly.Hat.provideFunction_("mathRandomInt", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(a, b) {", "  if (a > b) {", "    // Swap a and b to ensure a is smaller.", "    var c = a;", "    a = b;", "    b = c;", "  }", "  return Math.floor(Math.random() * (b - a + 1) + a);",
        "}"
    ]) + "(" + b + ", " + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.math_random_float = function (a) {
    return ["Math.random()", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.procedures = {};
Blockly.Hat.procedures_defreturn = function (a) {
    var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("NAME"), Blockly.Procedures.NAME_TYPE),
        c = Blockly.Hat.statementToCode(a, "STACK");
    if (Blockly.Hat.STATEMENT_PREFIX) {
        var d = a.id.replace(/\$/g, "$$$$");
        c = Blockly.Hat.prefixLines(Blockly.Hat.STATEMENT_PREFIX.replace(/%1/g, "'" + d + "'"), Blockly.Hat.INDENT) + c
    }
    Blockly.Hat.INFINITE_LOOP_TRAP && (c = Blockly.Hat.INFINITE_LOOP_TRAP.replace(/%1/g, "'" + a.id + "'") +
        c);
    (d = Blockly.Hat.valueToCode(a, "RETURN", Blockly.Hat.ORDER_NONE) || "") && (d = Blockly.Hat.INDENT + "return " + d + ";\n");
    for (var e = [], f = 0; f < a.arguments_.length; f++) e[f] = Blockly.Hat.variableDB_.getName(a.arguments_[f], Blockly.Variables.NAME_TYPE);
    c = "function " + b + "(" + e.join(", ") + ") {\n" + c + d + "}";
    c = Blockly.Hat.scrub_(a, c);
    Blockly.Hat.definitions_["%" + b] = c;
    return null
};
Blockly.Hat.procedures_defnoreturn = Blockly.Hat.procedures_defreturn;
Blockly.Hat.procedures_callreturn = function (a) {
    for (var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("NAME"), Blockly.Procedures.NAME_TYPE), c = [], d = 0; d < a.arguments_.length; d++) c[d] = Blockly.Hat.valueToCode(a, "ARG" + d, Blockly.Hat.ORDER_COMMA) || "null";
    return [b + "(" + c.join(", ") + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.procedures_callnoreturn = function (a) {
    for (var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("NAME"), Blockly.Procedures.NAME_TYPE), c = [], d = 0; d < a.arguments_.length; d++) c[d] = Blockly.Hat.valueToCode(a, "ARG" + d, Blockly.Hat.ORDER_COMMA) || "null";
    return b + "(" + c.join(", ") + ");\n"
};
Blockly.Hat.procedures_ifreturn = function (a) {
    var b = "if (" + (Blockly.Hat.valueToCode(a, "CONDITION", Blockly.Hat.ORDER_NONE) || "false") + ") {\n";
    a.hasReturnValue_ ? (a = Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_NONE) || "null", b += Blockly.Hat.INDENT + "return " + a + ";\n") : b += Blockly.Hat.INDENT + "return;\n";
    return b + "}\n"
};
Blockly.Hat.texts = {};
Blockly.Hat.text = function (a) {
    return [Blockly.Hat.quote_(a.getFieldValue("TEXT")), Blockly.Hat.ORDER_ATOMIC]
};
Blockly.Hat.text_join = function (a) {
    switch (a.itemCount_) {
        case 0:
            return ["''", Blockly.Hat.ORDER_ATOMIC];
        case 1:
            return ["String(" + (Blockly.Hat.valueToCode(a, "ADD0", Blockly.Hat.ORDER_NONE) || "''") + ")", Blockly.Hat.ORDER_FUNCTION_CALL];
        case 2:
            var b = Blockly.Hat.valueToCode(a, "ADD0", Blockly.Hat.ORDER_NONE) || "''";
            a = Blockly.Hat.valueToCode(a, "ADD1", Blockly.Hat.ORDER_NONE) || "''";
            return ["String(" + b + ") + String(" + a + ")", Blockly.Hat.ORDER_ADDITION];
        default:
            b = Array(a.itemCount_);
            for (var c = 0; c < a.itemCount_; c++) b[c] = Blockly.Hat.valueToCode(a, "ADD" + c, Blockly.Hat.ORDER_COMMA) || "''";
            a = "[" + b.join(",") + "].join('')";
            return [a, Blockly.Hat.ORDER_FUNCTION_CALL]
    }
};
Blockly.Hat.text_append = function (a) {
    var b = Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE);
    a = Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_NONE) || "''";
    return b + " = String(" + b + ") + String(" + a + ");\n"
};
Blockly.Hat.text_length = function (a) {
    return [(Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_FUNCTION_CALL) || "''") + ".length", Blockly.Hat.ORDER_MEMBER]
};
Blockly.Hat.text_isEmpty = function (a) {
    return ["!" + (Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_MEMBER) || "''") + ".length", Blockly.Hat.ORDER_LOGICAL_NOT]
};
Blockly.Hat.text_indexOf = function (a) {
    var b = "FIRST" == a.getFieldValue("END") ? "indexOf" : "lastIndexOf",
        c = Blockly.Hat.valueToCode(a, "FIND", Blockly.Hat.ORDER_NONE) || "''";
    b = (Blockly.Hat.valueToCode(a, "VALUE", Blockly.Hat.ORDER_MEMBER) || "''") + "." + b + "(" + c + ")";
    return a.workspace.options.oneBasedIndex ? [b + " + 1", Blockly.Hat.ORDER_ADDITION] : [b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.text_charAt = function (a) {
    var b = a.getFieldValue("WHERE") || "FROM_START",
        c = Blockly.Hat.valueToCode(a, "VALUE", "RANDOM" == b ? Blockly.Hat.ORDER_NONE : Blockly.Hat.ORDER_MEMBER) || "''";
    switch (b) {
        case "FIRST":
            return [c + ".charAt(0)", Blockly.Hat.ORDER_FUNCTION_CALL];
        case "LAST":
            return [c + ".slice(-1)", Blockly.Hat.ORDER_FUNCTION_CALL];
        case "FROM_START":
            return a = Blockly.Hat.getAdjusted(a, "AT"), [c + ".charAt(" + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL];
        case "FROM_END":
            return a =
                Blockly.Hat.getAdjusted(a, "AT", 1, !0), [c + ".slice(" + a + ").charAt(0)", Blockly.Hat.ORDER_FUNCTION_CALL];
        case "RANDOM":
            return [Blockly.Hat.provideFunction_("textRandomLetter", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(text) {", "  var x = Math.floor(Math.random() * text.length);", "  return text[x];", "}"]) + "(" + c + ")", Blockly.Hat.ORDER_FUNCTION_CALL]
    }
    throw "Unhandled option (text_charAt).";
};
Blockly.Hat.text.getIndex_ = function (a, b, c) {
    return "FIRST" == b ? "0" : "FROM_END" == b ? a + ".length - 1 - " + c : "LAST" == b ? a + ".length - 1" : c
};
Blockly.Hat.text_getSubstring = function (a) {
    var b = Blockly.Hat.valueToCode(a, "STRING", Blockly.Hat.ORDER_FUNCTION_CALL) || "''",
        c = a.getFieldValue("WHERE1"),
        d = a.getFieldValue("WHERE2");
    if ("FIRST" != c || "LAST" != d)
        if (b.match(/^'?\w+'?$/) || "FROM_END" != c && "LAST" != c && "FROM_END" != d && "LAST" != d) {
            switch (c) {
                case "FROM_START":
                    var e = Blockly.Hat.getAdjusted(a, "AT1");
                    break;
                case "FROM_END":
                    e = Blockly.Hat.getAdjusted(a, "AT1", 1, !1, Blockly.Hat.ORDER_SUBTRACTION);
                    e = b + ".length - " + e;
                    break;
                case "FIRST":
                    e = "0";
                    break;
                default:
                    throw "Unhandled option (text_getSubstring).";
            }
            switch (d) {
                case "FROM_START":
                    a = Blockly.Hat.getAdjusted(a, "AT2", 1);
                    break;
                case "FROM_END":
                    a = Blockly.Hat.getAdjusted(a, "AT2", 0, !1, Blockly.Hat.ORDER_SUBTRACTION);
                    a = b + ".length - " + a;
                    break;
                case "LAST":
                    a = b + ".length";
                    break;
                default:
                    throw "Unhandled option (text_getSubstring).";
            }
            b = b + ".slice(" + e + ", " + a + ")"
        } else {
            e = Blockly.Hat.getAdjusted(a, "AT1");
            a = Blockly.Hat.getAdjusted(a, "AT2");
            var f = Blockly.Hat.text.getIndex_,
                g = {
                    FIRST: "First",
                    LAST: "Last",
                    FROM_START: "FromStart",
                    FROM_END: "FromEnd"
                };
            b = Blockly.Hat.provideFunction_("subsequence" + g[c] + g[d], ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(sequence" + ("FROM_END" == c || "FROM_START" == c ? ", at1" : "") + ("FROM_END" == d || "FROM_START" == d ? ", at2" : "") + ") {", "  var start = " + f("sequence", c, "at1") + ";", "  var end = " + f("sequence", d, "at2") + " + 1;", "  return sequence.slice(start, end);", "}"]) + "(" + b + ("FROM_END" == c || "FROM_START" == c ? ", " + e : "") + ("FROM_END" == d || "FROM_START" ==
                d ? ", " + a : "") + ")"
        } return [b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.text_changeCase = function (a) {
    var b = {
        UPPERCASE: ".toUpperCase()",
        LOWERCASE: ".toLowerCase()",
        TITLECASE: null
    } [a.getFieldValue("CASE")];
    a = Blockly.Hat.valueToCode(a, "TEXT", b ? Blockly.Hat.ORDER_MEMBER : Blockly.Hat.ORDER_NONE) || "''";
    return [b ? a + b : Blockly.Hat.provideFunction_("textToTitleCase", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(str) {", "  return str.replace(/\\S+/g,", "      function(txt) {return txt[0].toUpperCase() + txt.substring(1).toLowerCase();});", "}"]) +
        "(" + a + ")", Blockly.Hat.ORDER_FUNCTION_CALL
    ]
};
Blockly.Hat.text_trim = function (a) {
    var b = {
        LEFT: ".replace(/^[\\s\\xa0]+/, '')",
        RIGHT: ".replace(/[\\s\\xa0]+$/, '')",
        BOTH: ".trim()"
    } [a.getFieldValue("MODE")];
    return [(Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_MEMBER) || "''") + b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.text_print = function (a) {
    return "window.alert(" + (Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_NONE) || "''") + ");\n"
};
Blockly.Hat.text_prompt_ext = function (a) {
    var b = "window.prompt(" + (a.getField("TEXT") ? Blockly.Hat.quote_(a.getFieldValue("TEXT")) : Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_NONE) || "''") + ")";
    "NUMBER" == a.getFieldValue("TYPE") && (b = "parseFloat(" + b + ")");
    return [b, Blockly.Hat.ORDER_FUNCTION_CALL]
};
Blockly.Hat.text_prompt = Blockly.Hat.text_prompt_ext;
Blockly.Hat.text_count = function (a) {
    var b = Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_MEMBER) || "''";
    a = Blockly.Hat.valueToCode(a, "SUB", Blockly.Hat.ORDER_NONE) || "''";
    return [Blockly.Hat.provideFunction_("textCount", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(haystack, needle) {", "  if (needle.length === 0) {", "    return haystack.length + 1;", "  } else {", "    return haystack.split(needle).length - 1;", "  }", "}"]) + "(" + b + ", " + a + ")", Blockly.Hat.ORDER_SUBTRACTION]
};
Blockly.Hat.text_replace = function (a) {
    var b = Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_MEMBER) || "''",
        c = Blockly.Hat.valueToCode(a, "FROM", Blockly.Hat.ORDER_NONE) || "''";
    a = Blockly.Hat.valueToCode(a, "TO", Blockly.Hat.ORDER_NONE) || "''";
    return [Blockly.Hat.provideFunction_("textReplace", ["function " + Blockly.Hat.FUNCTION_NAME_PLACEHOLDER_ + "(haystack, needle, replacement) {", '  needle = needle.replace(/([-()\\[\\]{}+?*.$\\^|,:#<!\\\\])/g,"\\\\$1")',
        '                 .replace(/\\x08/g,"\\\\x08");', "  return haystack.replace(new RegExp(needle, 'g'), replacement);", "}"
    ]) + "(" + b + ", " + c + ", " + a + ")", Blockly.Hat.ORDER_MEMBER]
};
Blockly.Hat.text_reverse = function (a) {
    return [(Blockly.Hat.valueToCode(a, "TEXT", Blockly.Hat.ORDER_MEMBER) || "''") + ".split('').reverse().join('')", Blockly.Hat.ORDER_MEMBER]
};
Blockly.Hat.variables = {};
//変数定義　変更点
Blockly.Hat.variables_get = function (a) {
//戻り値の関数名で表示　スペースで区切る
    return Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE) + " ";
};
Blockly.Hat.variables_set = function (a) {
    var b = Blockly.Hat.statementToCode(a, "VALUE", Blockly.Hat.ORDER_FUNCTION_CALL) || "0";
    return b + " " + "^" + "(" + Blockly.Hat.variableDB_.getName(a.getFieldValue("VAR"), Blockly.Variables.NAME_TYPE) + ")\n";
};